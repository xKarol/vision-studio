---
import { storyblokEditable } from "@storyblok/astro";
import HeadingComponent from "~/components/ui/Heading.astro";

const { blok } = Astro.props;
---

<HeadingComponent {...storyblokEditable(blok)}>
  {blok.text}
</HeadingComponent>

<!-- <Fragment
  _Heading
  {...storyblokEditable(blok)}
  class="w-full rounded-[5px] bg-[#f7f6fd] p-12 text-center"
>
  <h3 class="text-2xl font-bold text-[#1d243d]">{blok.name}</h3>
</Fragment> -->
